<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mallease.pms.dao.PmsSkuMemberPriceDao">

    <resultMap id="BaseResultMap" type="com.mallease.pms.pojo.PmsSkuMemberPrice">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="sku_id" property="skuId" jdbcType="BIGINT"/>
        <result column="member_level_id" property="memberLevelId" jdbcType="BIGINT"/>
        <result column="member_level_name" property="memberLevelName" jdbcType="VARCHAR"/>
        <result column="member_price" property="memberPrice" jdbcType="DECIMAL"/>
        <result column="creator" property="creator" jdbcType="VARCHAR"/>
        <result column="updater" property="updater" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, sku_id, member_level_id, member_level_name, member_price,
        creator, updater, create_time, update_time
    </sql>

    <!-- 根据主键查询 -->
    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pms_sku_member_price
        WHERE id = #{id,jdbcType=BIGINT}
    </select>

    <!-- 根据SKU ID查询所有会员价格 -->
    <select id="selectBySkuId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pms_sku_member_price
        WHERE sku_id = #{skuId,jdbcType=BIGINT}
        ORDER BY member_level_id
    </select>

    <!-- 根据SKU ID列表批量查询 -->
    <select id="selectBySkuIds" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pms_sku_member_price
        WHERE sku_id IN
        <foreach collection="skuIds" item="skuId" open="(" separator="," close=")">
            #{skuId,jdbcType=BIGINT}
        </foreach>
        ORDER BY sku_id, member_level_id
    </select>

    <!-- 根据SKU ID和会员等级ID查询 -->
    <select id="selectBySkuIdAndMemberLevelId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pms_sku_member_price
        WHERE sku_id = #{skuId,jdbcType=BIGINT}
          AND member_level_id = #{memberLevelId,jdbcType=BIGINT}
    </select>

    <!-- 插入 -->
    <insert id="insert" parameterType="com.mallease.pms.pojo.PmsSkuMemberPrice" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO pms_sku_member_price (
            sku_id, member_level_id, member_level_name, member_price, creator, updater
        ) VALUES (
            #{skuId,jdbcType=BIGINT}, #{memberLevelId,jdbcType=BIGINT},
            #{memberLevelName,jdbcType=VARCHAR}, #{memberPrice,jdbcType=DECIMAL},
            #{creator,jdbcType=VARCHAR}, #{updater,jdbcType=VARCHAR}
        )
    </insert>

    <!-- 选择性插入 -->
    <insert id="insertSelective" parameterType="com.mallease.pms.pojo.PmsSkuMemberPrice" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO pms_sku_member_price
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="skuId != null">sku_id,</if>
            <if test="memberLevelId != null">member_level_id,</if>
            <if test="memberLevelName != null">member_level_name,</if>
            <if test="memberPrice != null">member_price,</if>
            <if test="creator != null">creator,</if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            <if test="skuId != null">#{skuId,jdbcType=BIGINT},</if>
            <if test="memberLevelId != null">#{memberLevelId,jdbcType=BIGINT},</if>
            <if test="memberLevelName != null">#{memberLevelName,jdbcType=VARCHAR},</if>
            <if test="memberPrice != null">#{memberPrice,jdbcType=DECIMAL},</if>
            <if test="creator != null">#{creator,jdbcType=VARCHAR},</if>
        </trim>
    </insert>

    <!-- 批量插入 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO pms_sku_member_price (
            sku_id, member_level_id, member_level_name, member_price, creator
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.skuId,jdbcType=BIGINT}, #{item.memberLevelId,jdbcType=BIGINT},
                #{item.memberLevelName,jdbcType=VARCHAR}, #{item.memberPrice,jdbcType=DECIMAL},
                #{item.creator,jdbcType=VARCHAR}
            )
        </foreach>
    </insert>

    <!-- 根据主键更新 -->
    <update id="updateByPrimaryKey" parameterType="com.mallease.pms.pojo.PmsSkuMemberPrice">
        UPDATE pms_sku_member_price
        SET sku_id = #{skuId,jdbcType=BIGINT},
            member_level_id = #{memberLevelId,jdbcType=BIGINT},
            member_level_name = #{memberLevelName,jdbcType=VARCHAR},
            member_price = #{memberPrice,jdbcType=DECIMAL},
            updater = #{updater,jdbcType=VARCHAR}
        WHERE id = #{id,jdbcType=BIGINT}
    </update>

    <!-- 根据主键选择性更新 -->
    <update id="updateByPrimaryKeySelective" parameterType="com.mallease.pms.pojo.PmsSkuMemberPrice">
        UPDATE pms_sku_member_price
        <set>
            <if test="skuId != null">sku_id = #{skuId,jdbcType=BIGINT},</if>
            <if test="memberLevelId != null">member_level_id = #{memberLevelId,jdbcType=BIGINT},</if>
            <if test="memberLevelName != null">member_level_name = #{memberLevelName,jdbcType=VARCHAR},</if>
            <if test="memberPrice != null">member_price = #{memberPrice,jdbcType=DECIMAL},</if>
            <if test="updater != null">updater = #{updater,jdbcType=VARCHAR},</if>
        </set>
        WHERE id = #{id,jdbcType=BIGINT}
    </update>

    <!-- 根据SKU ID删除 -->
    <delete id="deleteBySkuId" parameterType="java.lang.Long">
        DELETE FROM pms_sku_member_price
        WHERE sku_id = #{skuId,jdbcType=BIGINT}
    </delete>

    <!-- 批量删除 -->
    <delete id="deleteBatch">
        DELETE FROM pms_sku_member_price
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id,jdbcType=BIGINT}
        </foreach>
    </delete>

</mapper>