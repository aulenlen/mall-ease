<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mallease.pms.dao.PmsSpuAttributeValueDao">

    <resultMap id="BaseResultMap" type="com.mallease.pms.pojo.PmsSpuAttributeValue">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="spu_id" property="spuId" jdbcType="BIGINT"/>
        <result column="product_attribute_id" property="productAttributeId" jdbcType="BIGINT"/>
        <result column="value" property="value" jdbcType="VARCHAR"/>
        <result column="deleted" property="deleted" jdbcType="TINYINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, spu_id, product_attribute_id, value, deleted, create_time, update_time
    </sql>

    <!-- 根据主键查询 -->
    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pms_spu_attribute_value
        WHERE id = #{id,jdbcType=BIGINT} AND deleted = 0
    </select>

    <!-- 根据SPU ID查询所有参数值 -->
    <select id="selectBySpuId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pms_spu_attribute_value
        WHERE spu_id = #{spuId,jdbcType=BIGINT} AND deleted = 0
        ORDER BY product_attribute_id
    </select>

    <!-- 根据SPU ID列表批量查询 -->
    <select id="selectBySpuIds" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pms_spu_attribute_value
        WHERE deleted = 0 AND spu_id IN
        <foreach collection="spuIds" item="spuId" open="(" separator="," close=")">
            #{spuId,jdbcType=BIGINT}
        </foreach>
        ORDER BY spu_id, product_attribute_id
    </select>

    <!-- 根据属性ID查询 -->
    <select id="selectByAttributeId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM pms_spu_attribute_value
        WHERE product_attribute_id = #{productAttributeId,jdbcType=BIGINT} AND deleted = 0
    </select>

    <!-- 插入 -->
    <insert id="insert" parameterType="com.mallease.pms.pojo.PmsSpuAttributeValue" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO pms_spu_attribute_value (
            spu_id, product_attribute_id, value, deleted
        ) VALUES (
            #{spuId,jdbcType=BIGINT}, #{productAttributeId,jdbcType=BIGINT},
            #{value,jdbcType=VARCHAR}, 0
        )
    </insert>

    <!-- 选择性插入 -->
    <insert id="insertSelective" parameterType="com.mallease.pms.pojo.PmsSpuAttributeValue" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO pms_spu_attribute_value
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="spuId != null">spu_id,</if>
            <if test="productAttributeId != null">product_attribute_id,</if>
            <if test="value != null">value,</if>
            deleted,
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            <if test="spuId != null">#{spuId,jdbcType=BIGINT},</if>
            <if test="productAttributeId != null">#{productAttributeId,jdbcType=BIGINT},</if>
            <if test="value != null">#{value,jdbcType=VARCHAR},</if>
            0,
        </trim>
    </insert>

    <!-- 批量插入 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO pms_spu_attribute_value (
            spu_id, product_attribute_id, value, deleted
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.spuId,jdbcType=BIGINT}, #{item.productAttributeId,jdbcType=BIGINT},
                #{item.value,jdbcType=VARCHAR}, 0
            )
        </foreach>
    </insert>

    <!-- 根据主键更新 -->
    <update id="updateByPrimaryKey" parameterType="com.mallease.pms.pojo.PmsSpuAttributeValue">
        UPDATE pms_spu_attribute_value
        SET spu_id = #{spuId,jdbcType=BIGINT},
            product_attribute_id = #{productAttributeId,jdbcType=BIGINT},
            value = #{value,jdbcType=VARCHAR}
        WHERE id = #{id,jdbcType=BIGINT} AND deleted = 0
    </update>

    <!-- 根据主键选择性更新 -->
    <update id="updateByPrimaryKeySelective" parameterType="com.mallease.pms.pojo.PmsSpuAttributeValue">
        UPDATE pms_spu_attribute_value
        <set>
            <if test="spuId != null">spu_id = #{spuId,jdbcType=BIGINT},</if>
            <if test="productAttributeId != null">product_attribute_id = #{productAttributeId,jdbcType=BIGINT},</if>
            <if test="value != null">value = #{value,jdbcType=VARCHAR},</if>
        </set>
        WHERE id = #{id,jdbcType=BIGINT} AND deleted = 0
    </update>

    <!-- 根据SPU ID逻辑删除 -->
    <update id="deleteBySpuId" parameterType="java.lang.Long">
        UPDATE pms_spu_attribute_value
        SET deleted = 1
        WHERE spu_id = #{spuId,jdbcType=BIGINT}
    </update>

    <!-- 批量逻辑删除 -->
    <update id="deleteBatch">
        UPDATE pms_spu_attribute_value
        SET deleted = 1
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id,jdbcType=BIGINT}
        </foreach>
    </update>

</mapper>