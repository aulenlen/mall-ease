# =================================================================
# Search TiDB Cloud / 生产环境配置
# 敏感信息通过环境变量注入
# =================================================================

spring:
  elasticsearch:
    rest:
      uris: ${ES_URIS}

logging:
  level:
    com.mallease: INFO
