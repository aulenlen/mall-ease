<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mallease.content.dal.mapper.EditorialDao">
    <resultMap id="BaseResultMap" type="com.mallease.content.dal.entity.Editorial">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="series_name" property="seriesName" jdbcType="VARCHAR"/>
        <result column="volume_no" property="volumeNo" jdbcType="INTEGER"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="sub_title" property="subTitle" jdbcType="VARCHAR"/>
        <result column="cover_pic" property="coverPic" jdbcType="VARCHAR"/>
        <result column="content" property="content" jdbcType="LONGVARCHAR"/>
        <result column="author" property="author" jdbcType="VARCHAR"/>
        <result column="sort" property="sort" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="publish_time" property="publishTime" jdbcType="TIMESTAMP"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="deleted" property="deleted" jdbcType="TINYINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, series_name, volume_no, title, sub_title, cover_pic, content, author,
        sort, status, publish_time, create_time, update_time, deleted
    </sql>

    <!-- 根据主键查询 -->
    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from content_editorial
        where id = #{id,jdbcType=BIGINT} and deleted = 0
    </select>

    <!-- 根据主键查询已发布详情 -->
    <select id="selectPublishedById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from content_editorial
        where id = #{id,jdbcType=BIGINT}
          and deleted = 0
          and status = 1
    </select>

    <!-- 条件查询列表 -->
    <select id="selectByQuery" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from content_editorial
        <where>
            deleted = 0
            <if test="query.keyword != null and query.keyword != ''">
                AND title LIKE CONCAT('%', #{query.keyword,jdbcType=VARCHAR}, '%')
            </if>
            <if test="query.seriesName != null and query.seriesName != ''">
                AND series_name = #{query.seriesName,jdbcType=VARCHAR}
            </if>
            <if test="query.status != null">
                AND status = #{query.status,jdbcType=TINYINT}
            </if>
        </where>
        order by sort asc, create_time desc
    </select>

    <!-- 批量更新状态 -->
    <update id="updateStatusBatch">
        update content_editorial
        set status = #{status,jdbcType=TINYINT},
            update_time = NOW()
        where deleted = 0 and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id,jdbcType=BIGINT}
        </foreach>
    </update>

    <!-- 逻辑删除 -->
    <update id="logicDeleteByPrimaryKey" parameterType="java.lang.Long">
        update content_editorial
        set deleted = 1,
            update_time = NOW()
        where id = #{id,jdbcType=BIGINT}
    </update>

    <!-- 批量逻辑删除 -->
    <update id="logicDeleteBatch">
        update content_editorial
        set deleted = 1,
            update_time = NOW()
        where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id,jdbcType=BIGINT}
        </foreach>
    </update>

    <!-- 插入 -->
    <insert id="insert" parameterType="com.mallease.content.dal.entity.Editorial" useGeneratedKeys="true" keyProperty="id">
        insert into content_editorial (series_name, volume_no, title, sub_title, cover_pic,
                                       content, author, sort, status, publish_time,
                                       create_time, update_time, deleted)
        values (#{seriesName,jdbcType=VARCHAR}, #{volumeNo,jdbcType=INTEGER}, #{title,jdbcType=VARCHAR},
                #{subTitle,jdbcType=VARCHAR}, #{coverPic,jdbcType=VARCHAR}, #{content,jdbcType=LONGVARCHAR},
                #{author,jdbcType=VARCHAR}, #{sort,jdbcType=INTEGER}, #{status,jdbcType=TINYINT},
                #{publishTime,jdbcType=TIMESTAMP}, #{createTime,jdbcType=TIMESTAMP},
                #{updateTime,jdbcType=TIMESTAMP}, #{deleted,jdbcType=TINYINT})
    </insert>

    <!-- 选择性插入 -->
    <insert id="insertSelective" parameterType="com.mallease.content.dal.entity.Editorial" useGeneratedKeys="true" keyProperty="id">
        insert into content_editorial
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="seriesName != null">series_name,</if>
            <if test="volumeNo != null">volume_no,</if>
            <if test="title != null">title,</if>
            <if test="subTitle != null">sub_title,</if>
            <if test="coverPic != null">cover_pic,</if>
            <if test="content != null">content,</if>
            <if test="author != null">author,</if>
            <if test="sort != null">sort,</if>
            <if test="status != null">status,</if>
            <if test="publishTime != null">publish_time,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="deleted != null">deleted,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="seriesName != null">#{seriesName,jdbcType=VARCHAR},</if>
            <if test="volumeNo != null">#{volumeNo,jdbcType=INTEGER},</if>
            <if test="title != null">#{title,jdbcType=VARCHAR},</if>
            <if test="subTitle != null">#{subTitle,jdbcType=VARCHAR},</if>
            <if test="coverPic != null">#{coverPic,jdbcType=VARCHAR},</if>
            <if test="content != null">#{content,jdbcType=LONGVARCHAR},</if>
            <if test="author != null">#{author,jdbcType=VARCHAR},</if>
            <if test="sort != null">#{sort,jdbcType=INTEGER},</if>
            <if test="status != null">#{status,jdbcType=TINYINT},</if>
            <if test="publishTime != null">#{publishTime,jdbcType=TIMESTAMP},</if>
            <if test="createTime != null">#{createTime,jdbcType=TIMESTAMP},</if>
            <if test="updateTime != null">#{updateTime,jdbcType=TIMESTAMP},</if>
            <if test="deleted != null">#{deleted,jdbcType=TINYINT},</if>
        </trim>
    </insert>

    <!-- 选择性更新 -->
    <update id="updateByPrimaryKeySelective" parameterType="com.mallease.content.dal.entity.Editorial">
        update content_editorial
        <set>
            <if test="seriesName != null">series_name = #{seriesName,jdbcType=VARCHAR},</if>
            <if test="volumeNo != null">volume_no = #{volumeNo,jdbcType=INTEGER},</if>
            <if test="title != null">title = #{title,jdbcType=VARCHAR},</if>
            <if test="subTitle != null">sub_title = #{subTitle,jdbcType=VARCHAR},</if>
            <if test="coverPic != null">cover_pic = #{coverPic,jdbcType=VARCHAR},</if>
            <if test="content != null">content = #{content,jdbcType=LONGVARCHAR},</if>
            <if test="author != null">author = #{author,jdbcType=VARCHAR},</if>
            <if test="sort != null">sort = #{sort,jdbcType=INTEGER},</if>
            <if test="status != null">status = #{status,jdbcType=TINYINT},</if>
            <if test="publishTime != null">publish_time = #{publishTime,jdbcType=TIMESTAMP},</if>
            update_time = NOW(),
        </set>
        where id = #{id,jdbcType=BIGINT} and deleted = 0
    </update>

    <!-- 查询已发布的编辑精选列表 -->
    <select id="listPublished" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from content_editorial
        where deleted = 0 and status = 1
        order by sort asc, publish_time desc
        limit #{limit}
    </select>
</mapper>
