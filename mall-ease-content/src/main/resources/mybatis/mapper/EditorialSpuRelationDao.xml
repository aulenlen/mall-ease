<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mallease.content.dal.mapper.EditorialSpuRelationDao">
    <resultMap id="BaseResultMap" type="com.mallease.content.dal.entity.EditorialSpuRelation">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="editorial_id" property="editorialId" jdbcType="BIGINT"/>
        <result column="spu_id" property="spuId" jdbcType="BIGINT"/>
        <result column="sort" property="sort" jdbcType="INTEGER"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, editorial_id, spu_id, sort, create_time
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from content_editorial_spu_relation
        where id = #{id,jdbcType=BIGINT}
    </select>

    <select id="selectByEditorialId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from content_editorial_spu_relation
        where editorial_id = #{editorialId,jdbcType=BIGINT}
        order by sort asc
    </select>

    <select id="selectByEditorialIds" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from content_editorial_spu_relation
        where editorial_id in
        <foreach collection="editorialIds" item="editorialId" open="(" separator="," close=")">
            #{editorialId,jdbcType=BIGINT}
        </foreach>
        order by editorial_id, sort asc
    </select>

    <select id="selectBySpuId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from content_editorial_spu_relation
        where spu_id = #{spuId,jdbcType=BIGINT}
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from content_editorial_spu_relation
        where id = #{id,jdbcType=BIGINT}
    </delete>

    <delete id="deleteByEditorialId">
        delete from content_editorial_spu_relation
        where editorial_id = #{editorialId,jdbcType=BIGINT}
    </delete>

    <delete id="deleteBySpuId">
        delete from content_editorial_spu_relation
        where spu_id = #{spuId,jdbcType=BIGINT}
    </delete>

    <insert id="insert" parameterType="com.mallease.content.dal.entity.EditorialSpuRelation">
        insert into content_editorial_spu_relation (id, editorial_id, spu_id, sort, create_time)
        values (#{id,jdbcType=BIGINT}, #{editorialId,jdbcType=BIGINT}, #{spuId,jdbcType=BIGINT},
                #{sort,jdbcType=INTEGER}, #{createTime,jdbcType=TIMESTAMP})
    </insert>

    <insert id="insertSelective" parameterType="com.mallease.content.dal.entity.EditorialSpuRelation">
        insert into content_editorial_spu_relation
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="editorialId != null">editorial_id,</if>
            <if test="spuId != null">spu_id,</if>
            <if test="sort != null">sort,</if>
            <if test="createTime != null">create_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id,jdbcType=BIGINT},</if>
            <if test="editorialId != null">#{editorialId,jdbcType=BIGINT},</if>
            <if test="spuId != null">#{spuId,jdbcType=BIGINT},</if>
            <if test="sort != null">#{sort,jdbcType=INTEGER},</if>
            <if test="createTime != null">#{createTime,jdbcType=TIMESTAMP},</if>
        </trim>
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        insert into content_editorial_spu_relation (id, editorial_id, spu_id, sort, create_time)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.id,jdbcType=BIGINT}, #{item.editorialId,jdbcType=BIGINT}, #{item.spuId,jdbcType=BIGINT},
             #{item.sort,jdbcType=INTEGER}, #{item.createTime,jdbcType=TIMESTAMP})
        </foreach>
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="com.mallease.content.dal.entity.EditorialSpuRelation">
        update content_editorial_spu_relation
        <set>
            <if test="editorialId != null">editorial_id = #{editorialId,jdbcType=BIGINT},</if>
            <if test="spuId != null">spu_id = #{spuId,jdbcType=BIGINT},</if>
            <if test="sort != null">sort = #{sort,jdbcType=INTEGER},</if>
            <if test="createTime != null">create_time = #{createTime,jdbcType=TIMESTAMP},</if>
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>

    <update id="updateByPrimaryKey" parameterType="com.mallease.content.dal.entity.EditorialSpuRelation">
        update content_editorial_spu_relation
        set editorial_id = #{editorialId,jdbcType=BIGINT},
            spu_id = #{spuId,jdbcType=BIGINT},
            sort = #{sort,jdbcType=INTEGER},
            create_time = #{createTime,jdbcType=TIMESTAMP}
        where id = #{id,jdbcType=BIGINT}
    </update>

    <!-- 根据文章ID和商品ID列表删除 -->
    <delete id="deleteByEditorialIdAndSpuIds">
        delete from content_editorial_spu_relation
        where editorial_id = #{editorialId,jdbcType=BIGINT}
          and spu_id in
        <foreach collection="spuIds" item="spuId" open="(" separator="," close=")">
            #{spuId,jdbcType=BIGINT}
        </foreach>
    </delete>
</mapper>
