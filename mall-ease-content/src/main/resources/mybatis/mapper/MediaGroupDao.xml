<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mallease.content.dal.mapper.MediaGroupDao">
    <resultMap id="BaseResultMap" type="com.mallease.content.dal.entity.MediaGroup">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="sort" property="sort" jdbcType="INTEGER"/>
        <result column="deleted" property="deleted" jdbcType="TINYINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, sort, deleted, create_time, update_time
    </sql>

    <insert id="insertSelective" parameterType="com.mallease.content.dal.entity.MediaGroup" useGeneratedKeys="true" keyProperty="id">
        insert into content_media_group
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="name != null">name,</if>
            <if test="sort != null">sort,</if>
            <if test="deleted != null">deleted,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="name != null">#{name,jdbcType=VARCHAR},</if>
            <if test="sort != null">#{sort,jdbcType=INTEGER},</if>
            <if test="deleted != null">#{deleted,jdbcType=TINYINT},</if>
            <if test="createTime != null">#{createTime,jdbcType=TIMESTAMP},</if>
            <if test="updateTime != null">#{updateTime,jdbcType=TIMESTAMP},</if>
        </trim>
    </insert>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from content_media_group
        where id = #{id,jdbcType=BIGINT}
          and deleted = 0
    </select>

    <select id="selectByName" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from content_media_group
        where name = #{name,jdbcType=VARCHAR}
          and deleted = 0
        limit 1
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from content_media_group
        where deleted = 0
        order by sort asc, create_time desc, id desc
    </select>

    <update id="updateByPrimaryKeySelective" parameterType="com.mallease.content.dal.entity.MediaGroup">
        update content_media_group
        <set>
            <if test="name != null">name = #{name,jdbcType=VARCHAR},</if>
            <if test="sort != null">sort = #{sort,jdbcType=INTEGER},</if>
            <if test="deleted != null">deleted = #{deleted,jdbcType=TINYINT},</if>
            update_time = now(),
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>

    <update id="logicDeleteByPrimaryKey" parameterType="java.lang.Long">
        update content_media_group
        set deleted = 1,
            update_time = now()
        where id = #{id,jdbcType=BIGINT}
          and deleted = 0
    </update>
</mapper>
