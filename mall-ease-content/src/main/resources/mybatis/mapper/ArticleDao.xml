<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mallease.content.dal.mapper.ArticleDao">
    <resultMap id="BaseResultMap" type="com.mallease.content.dal.entity.Article">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="sub_title" property="subTitle" jdbcType="VARCHAR"/>
        <result column="cover_pic" property="coverPic" jdbcType="VARCHAR"/>
        <result column="content" property="content" jdbcType="LONGVARCHAR"/>
        <result column="sort" property="sort" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="extras" property="extras" jdbcType="VARCHAR"/>
        <result column="publish_time" property="publishTime" jdbcType="TIMESTAMP"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="deleted" property="deleted" jdbcType="TINYINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, title, sub_title, cover_pic, content, sort, status, extras,
        publish_time, create_time, update_time, deleted
    </sql>

    <insert id="insertSelective" parameterType="com.mallease.content.dal.entity.Article" useGeneratedKeys="true" keyProperty="id">
        insert into content_article
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="title != null">title,</if>
            <if test="subTitle != null">sub_title,</if>
            <if test="coverPic != null">cover_pic,</if>
            <if test="content != null">content,</if>
            <if test="sort != null">sort,</if>
            <if test="status != null">status,</if>
            <if test="extras != null">extras,</if>
            <if test="publishTime != null">publish_time,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="deleted != null">deleted,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="title != null">#{title,jdbcType=VARCHAR},</if>
            <if test="subTitle != null">#{subTitle,jdbcType=VARCHAR},</if>
            <if test="coverPic != null">#{coverPic,jdbcType=VARCHAR},</if>
            <if test="content != null">#{content,jdbcType=LONGVARCHAR},</if>
            <if test="sort != null">#{sort,jdbcType=INTEGER},</if>
            <if test="status != null">#{status,jdbcType=TINYINT},</if>
            <if test="extras != null">#{extras,jdbcType=VARCHAR},</if>
            <if test="publishTime != null">#{publishTime,jdbcType=TIMESTAMP},</if>
            <if test="createTime != null">#{createTime,jdbcType=TIMESTAMP},</if>
            <if test="updateTime != null">#{updateTime,jdbcType=TIMESTAMP},</if>
            <if test="deleted != null">#{deleted,jdbcType=TINYINT},</if>
        </trim>
    </insert>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from content_article
        where id = #{id,jdbcType=BIGINT}
          and deleted = 0
    </select>

    <select id="selectPublishedById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from content_article
        where id = #{id,jdbcType=BIGINT}
          and deleted = 0
          and status = 1
    </select>

    <select id="selectByQuery" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from content_article
        <where>
            deleted = 0
            <if test="query.keyword != null and query.keyword != ''">
                and title like concat('%', #{query.keyword,jdbcType=VARCHAR}, '%')
            </if>
            <if test="query.status != null">
                and status = #{query.status,jdbcType=TINYINT}
            </if>
        </where>
        order by sort asc, create_time desc
    </select>

    <update id="updateByPrimaryKeySelective" parameterType="com.mallease.content.dal.entity.Article">
        update content_article
        <set>
            <if test="title != null">title = #{title,jdbcType=VARCHAR},</if>
            <if test="subTitle != null">sub_title = #{subTitle,jdbcType=VARCHAR},</if>
            <if test="coverPic != null">cover_pic = #{coverPic,jdbcType=VARCHAR},</if>
            <if test="content != null">content = #{content,jdbcType=LONGVARCHAR},</if>
            <if test="sort != null">sort = #{sort,jdbcType=INTEGER},</if>
            <if test="status != null">status = #{status,jdbcType=TINYINT},</if>
            <if test="extras != null">extras = #{extras,jdbcType=VARCHAR},</if>
            <if test="publishTime != null">publish_time = #{publishTime,jdbcType=TIMESTAMP},</if>
            update_time = NOW(),
        </set>
        where id = #{id,jdbcType=BIGINT}
          and deleted = 0
    </update>

    <update id="updateStatusBatch">
        update content_article
        set status = #{status,jdbcType=TINYINT},
        <if test="status == 1">
            publish_time = ifnull(publish_time, now()),
        </if>
            update_time = now()
        where deleted = 0
          and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id,jdbcType=BIGINT}
        </foreach>
    </update>

    <update id="logicDeleteByPrimaryKey" parameterType="java.lang.Long">
        update content_article
        set deleted = 1,
            update_time = now()
        where id = #{id,jdbcType=BIGINT}
    </update>

    <update id="logicDeleteBatch">
        update content_article
        set deleted = 1,
            update_time = now()
        where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id,jdbcType=BIGINT}
        </foreach>
    </update>

    <select id="listPublishedBySlotCode" resultMap="BaseResultMap">
        select
            a.id,
            a.title,
            a.sub_title,
            a.cover_pic,
            a.content,
            a.sort,
            a.status,
            a.extras,
            a.publish_time,
            a.create_time,
            a.update_time,
            a.deleted
        from content_article a
                 inner join content_slot_item si on si.article_id = a.id
                 inner join content_slot s on s.id = si.slot_id
        where a.deleted = 0
          and a.status = 1
          and s.deleted = 0
          and s.status = 1
          and s.code = #{slotCode,jdbcType=VARCHAR}
          and s.render_type = 'ARTICLE_LIST'
          and si.deleted = 0
          and si.status = 1
          and si.item_type = 'ARTICLE'
          and (si.start_time is null or si.start_time &lt;= now())
          and (si.end_time is null or si.end_time &gt; now())
        order by si.sort asc, a.publish_time desc, a.create_time desc
        <if test="limit != null">
            limit #{limit}
        </if>
    </select>
</mapper>
