<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mallease.product.dao.SpecValueDao">

    <resultMap id="BaseResultMap" type="com.mallease.product.model.data.entity.SpecValue">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="spec_id" property="specId" jdbcType="BIGINT"/>
        <result column="value" property="value" jdbcType="VARCHAR"/>
        <result column="image" property="image" jdbcType="VARCHAR"/>
        <result column="color_code" property="colorCode" jdbcType="VARCHAR"/>
        <result column="sort" property="sort" jdbcType="INTEGER"/>
        <result column="deleted" property="deleted" jdbcType="TINYINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, spec_id, value, image, color_code, sort, deleted, create_time, update_time
    </sql>

    <!-- 根据主键查询 -->
    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM product_spec_value
        WHERE id = #{id,jdbcType=BIGINT} AND deleted = 0
    </select>

    <!-- 根据ID列表批量查询 -->
    <select id="selectByIds" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM product_spec_value
        WHERE deleted = 0 AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id,jdbcType=BIGINT}
        </foreach>
        ORDER BY spec_id, sort, id
    </select>

    <!-- 根据规格ID查询 -->
    <select id="selectBySpecId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM product_spec_value
        WHERE spec_id = #{specId,jdbcType=BIGINT} AND deleted = 0
        ORDER BY sort, id
    </select>

    <!-- 根据规格ID列表批量查询 -->
    <select id="selectBySpecIds" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM product_spec_value
        WHERE deleted = 0 AND spec_id IN
        <foreach collection="specIds" item="specId" open="(" separator="," close=")">
            #{specId,jdbcType=BIGINT}
        </foreach>
        ORDER BY spec_id, sort, id
    </select>

    <!-- 根据规格ID和值查询 -->
    <select id="selectBySpecIdAndValue" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM product_spec_value
        WHERE spec_id = #{specId,jdbcType=BIGINT}
          AND value = #{value,jdbcType=VARCHAR}
          AND deleted = 0
    </select>

    <!-- 插入 -->
    <insert id="insert" parameterType="com.mallease.product.model.data.entity.SpecValue" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product_spec_value (
            spec_id, value, image, color_code, sort, deleted
        ) VALUES (
            #{specId,jdbcType=BIGINT}, #{value,jdbcType=VARCHAR}, #{image,jdbcType=VARCHAR},
            #{colorCode,jdbcType=VARCHAR}, #{sort,jdbcType=INTEGER}, 0
        )
    </insert>

    <!-- 选择性插入 -->
    <insert id="insertSelective" parameterType="com.mallease.product.model.data.entity.SpecValue" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product_spec_value
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="specId != null">spec_id,</if>
            <if test="value != null">value,</if>
            <if test="image != null">image,</if>
            <if test="colorCode != null">color_code,</if>
            <if test="sort != null">sort,</if>
            deleted,
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            <if test="specId != null">#{specId,jdbcType=BIGINT},</if>
            <if test="value != null">#{value,jdbcType=VARCHAR},</if>
            <if test="image != null">#{image,jdbcType=VARCHAR},</if>
            <if test="colorCode != null">#{colorCode,jdbcType=VARCHAR},</if>
            <if test="sort != null">#{sort,jdbcType=INTEGER},</if>
            0,
        </trim>
    </insert>

    <!-- 批量插入 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO product_spec_value (
            spec_id, value, image, color_code, sort, deleted
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.specId,jdbcType=BIGINT}, #{item.value,jdbcType=VARCHAR},
                #{item.image,jdbcType=VARCHAR}, #{item.colorCode,jdbcType=VARCHAR},
                #{item.sort,jdbcType=INTEGER}, 0
            )
        </foreach>
    </insert>

    <!-- 根据主键更新 -->
    <update id="updateByPrimaryKey" parameterType="com.mallease.product.model.data.entity.SpecValue">
        UPDATE product_spec_value
        SET spec_id = #{specId,jdbcType=BIGINT},
            value = #{value,jdbcType=VARCHAR},
            image = #{image,jdbcType=VARCHAR},
            color_code = #{colorCode,jdbcType=VARCHAR},
            sort = #{sort,jdbcType=INTEGER}
        WHERE id = #{id,jdbcType=BIGINT} AND deleted = 0
    </update>

    <!-- 根据主键选择性更新 -->
    <update id="updateByPrimaryKeySelective" parameterType="com.mallease.product.model.data.entity.SpecValue">
        UPDATE product_spec_value
        <set>
            <if test="specId != null">spec_id = #{specId,jdbcType=BIGINT},</if>
            <if test="value != null">value = #{value,jdbcType=VARCHAR},</if>
            <if test="image != null">image = #{image,jdbcType=VARCHAR},</if>
            <if test="colorCode != null">color_code = #{colorCode,jdbcType=VARCHAR},</if>
            <if test="sort != null">sort = #{sort,jdbcType=INTEGER},</if>
        </set>
        WHERE id = #{id,jdbcType=BIGINT} AND deleted = 0
    </update>

    <!-- 逻辑删除 -->
    <update id="deleteById" parameterType="java.lang.Long">
        UPDATE product_spec_value
        SET deleted = 1
        WHERE id = #{id,jdbcType=BIGINT}
    </update>

    <!-- 根据规格ID逻辑删除 -->
    <update id="deleteBySpecId" parameterType="java.lang.Long">
        UPDATE product_spec_value
        SET deleted = 1
        WHERE spec_id = #{specId,jdbcType=BIGINT}
    </update>

    <!-- 批量逻辑删除 -->
    <update id="deleteBatch">
        UPDATE product_spec_value
        SET deleted = 1
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id,jdbcType=BIGINT}
        </foreach>
    </update>

    <!-- 根据规格ID列表批量逻辑删除 -->
    <update id="deleteBySpecIds">
        UPDATE product_spec_value
        SET deleted = 1
        WHERE spec_id IN
        <foreach collection="specIds" item="specId" open="(" separator="," close=")">
            #{specId,jdbcType=BIGINT}
        </foreach>
    </update>

</mapper>