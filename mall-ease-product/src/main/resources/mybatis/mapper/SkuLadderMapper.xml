<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mallease.product.dao.SkuLadderDao">

    <resultMap id="BaseResultMap" type="com.mallease.product.model.data.entity.SkuLadder">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="sku_id" property="skuId" jdbcType="BIGINT"/>
        <result column="count" property="count" jdbcType="INTEGER"/>
        <result column="discount" property="discount" jdbcType="DECIMAL"/>
        <result column="price" property="price" jdbcType="DECIMAL"/>
        <result column="creator" property="creator" jdbcType="VARCHAR"/>
        <result column="updater" property="updater" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, sku_id, count, discount, price, creator, updater, create_time, update_time
    </sql>

    <!-- 根据主键查询 -->
    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM product_sku_ladder
        WHERE id = #{id,jdbcType=BIGINT}
    </select>

    <!-- 根据SKU ID查询所有阶梯价格 -->
    <select id="selectBySkuId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM product_sku_ladder
        WHERE sku_id = #{skuId,jdbcType=BIGINT}
        ORDER BY count ASC
    </select>

    <!-- 根据SKU ID列表批量查询 -->
    <select id="selectBySkuIds" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM product_sku_ladder
        WHERE sku_id IN
        <foreach collection="skuIds" item="skuId" open="(" separator="," close=")">
            #{skuId,jdbcType=BIGINT}
        </foreach>
        ORDER BY sku_id, count ASC
    </select>

    <!-- 查询满足数量的最优阶梯价格 -->
    <select id="selectBestLadder" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM product_sku_ladder
        WHERE sku_id = #{skuId,jdbcType=BIGINT}
          AND count &lt;= #{count,jdbcType=INTEGER}
        ORDER BY count DESC
        LIMIT 1
    </select>

    <!-- 插入 -->
    <insert id="insert" parameterType="com.mallease.product.model.data.entity.SkuLadder" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product_sku_ladder (
            sku_id, count, discount, price, creator, updater
        ) VALUES (
            #{skuId,jdbcType=BIGINT}, #{count,jdbcType=INTEGER}, #{discount,jdbcType=DECIMAL},
            #{price,jdbcType=DECIMAL}, #{creator,jdbcType=VARCHAR}, #{updater,jdbcType=VARCHAR}
        )
    </insert>

    <!-- 选择性插入 -->
    <insert id="insertSelective" parameterType="com.mallease.product.model.data.entity.SkuLadder" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product_sku_ladder
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="skuId != null">sku_id,</if>
            <if test="count != null">count,</if>
            <if test="discount != null">discount,</if>
            <if test="price != null">price,</if>
            <if test="creator != null">creator,</if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            <if test="skuId != null">#{skuId,jdbcType=BIGINT},</if>
            <if test="count != null">#{count,jdbcType=INTEGER},</if>
            <if test="discount != null">#{discount,jdbcType=DECIMAL},</if>
            <if test="price != null">#{price,jdbcType=DECIMAL},</if>
            <if test="creator != null">#{creator,jdbcType=VARCHAR},</if>
        </trim>
    </insert>

    <!-- 批量插入 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO product_sku_ladder (
            sku_id, count, discount, price, creator
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.skuId,jdbcType=BIGINT}, #{item.count,jdbcType=INTEGER},
                #{item.discount,jdbcType=DECIMAL}, #{item.price,jdbcType=DECIMAL},
                #{item.creator,jdbcType=VARCHAR}
            )
        </foreach>
    </insert>

    <!-- 根据主键更新 -->
    <update id="updateByPrimaryKey" parameterType="com.mallease.product.model.data.entity.SkuLadder">
        UPDATE product_sku_ladder
        SET sku_id = #{skuId,jdbcType=BIGINT},
            count = #{count,jdbcType=INTEGER},
            discount = #{discount,jdbcType=DECIMAL},
            price = #{price,jdbcType=DECIMAL},
            updater = #{updater,jdbcType=VARCHAR}
        WHERE id = #{id,jdbcType=BIGINT}
    </update>

    <!-- 根据主键选择性更新 -->
    <update id="updateByPrimaryKeySelective" parameterType="com.mallease.product.model.data.entity.SkuLadder">
        UPDATE product_sku_ladder
        <set>
            <if test="skuId != null">sku_id = #{skuId,jdbcType=BIGINT},</if>
            <if test="count != null">count = #{count,jdbcType=INTEGER},</if>
            <if test="discount != null">discount = #{discount,jdbcType=DECIMAL},</if>
            <if test="price != null">price = #{price,jdbcType=DECIMAL},</if>
            <if test="updater != null">updater = #{updater,jdbcType=VARCHAR},</if>
        </set>
        WHERE id = #{id,jdbcType=BIGINT}
    </update>

    <!-- 根据SKU ID删除 -->
    <delete id="deleteBySkuId" parameterType="java.lang.Long">
        DELETE FROM product_sku_ladder
        WHERE sku_id = #{skuId,jdbcType=BIGINT}
    </delete>

    <!-- 批量删除 -->
    <delete id="deleteBatch">
        DELETE FROM product_sku_ladder
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id,jdbcType=BIGINT}
        </foreach>
    </delete>

</mapper>