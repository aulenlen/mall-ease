<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mallease.user.dao.RoleResourceRelationDao">
    <resultMap id="BaseResultMap" type="com.mallease.user.model.data.RoleResourceRelation">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="role_id" property="roleId" jdbcType="BIGINT"/>
        <result column="resource_id" property="resourceId" jdbcType="BIGINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, role_id, resource_id
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from user_role_resource_relation
        where id = #{id,jdbcType=BIGINT}
    </select>

    <select id="selectByRoleId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from user_role_resource_relation
        where role_id = #{roleId,jdbcType=BIGINT}
    </select>

    <select id="selectByResourceId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from user_role_resource_relation
        where resource_id = #{resourceId,jdbcType=BIGINT}
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from user_role_resource_relation
        where id = #{id,jdbcType=BIGINT}
    </delete>

    <delete id="deleteByRoleId" parameterType="java.lang.Long">
        delete from user_role_resource_relation
        where role_id = #{roleId,jdbcType=BIGINT}
    </delete>

    <delete id="deleteByResourceId" parameterType="java.lang.Long">
        delete from user_role_resource_relation
        where resource_id = #{resourceId,jdbcType=BIGINT}
    </delete>

    <insert id="insert" parameterType="com.mallease.user.model.data.RoleResourceRelation" useGeneratedKeys="true" keyProperty="id">
        insert into user_role_resource_relation (id, role_id, resource_id)
        values (#{id,jdbcType=BIGINT}, #{roleId,jdbcType=BIGINT}, #{resourceId,jdbcType=BIGINT})
    </insert>

    <insert id="insertSelective" parameterType="com.mallease.user.model.data.RoleResourceRelation" useGeneratedKeys="true" keyProperty="id">
        insert into user_role_resource_relation
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="roleId != null">
                role_id,
            </if>
            <if test="resourceId != null">
                resource_id,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id,jdbcType=BIGINT},
            </if>
            <if test="roleId != null">
                #{roleId,jdbcType=BIGINT},
            </if>
            <if test="resourceId != null">
                #{resourceId,jdbcType=BIGINT},
            </if>
        </trim>
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        insert into user_role_resource_relation (role_id, resource_id)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.roleId,jdbcType=BIGINT}, #{item.resourceId,jdbcType=BIGINT})
        </foreach>
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="com.mallease.user.model.data.RoleResourceRelation">
        update user_role_resource_relation
        <set>
            <if test="roleId != null">
                role_id = #{roleId,jdbcType=BIGINT},
            </if>
            <if test="resourceId != null">
                resource_id = #{resourceId,jdbcType=BIGINT},
            </if>
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>

    <update id="updateByPrimaryKey" parameterType="com.mallease.user.model.data.RoleResourceRelation">
        update user_role_resource_relation
        set role_id = #{roleId,jdbcType=BIGINT},
            resource_id = #{resourceId,jdbcType=BIGINT}
        where id = #{id,jdbcType=BIGINT}
    </update>

    <select id="selectResourceIdsByRoleIds" resultType="java.lang.Long">
        SELECT DISTINCT resource_id
        FROM user_role_resource_relation
        WHERE role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId,jdbcType=BIGINT}
        </foreach>
    </select>

    <delete id="deleteByRoleIds">
        DELETE FROM user_role_resource_relation
        WHERE role_id IN
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId,jdbcType=BIGINT}
        </foreach>
    </delete>
</mapper>

