<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mallease.user.dal.mapper.MemberAddressDao">
    <resultMap id="BaseResultMap" type="com.mallease.user.dal.entity.MemberAddress">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="member_id" property="memberId" jdbcType="BIGINT"/>
        <result column="receiver_name" property="receiverName" jdbcType="VARCHAR"/>
        <result column="receiver_phone" property="receiverPhone" jdbcType="VARCHAR"/>
        <result column="province" property="province" jdbcType="VARCHAR"/>
        <result column="city" property="city" jdbcType="VARCHAR"/>
        <result column="district" property="district" jdbcType="VARCHAR"/>
        <result column="detail_address" property="detailAddress" jdbcType="VARCHAR"/>
        <result column="is_default" property="isDefault" jdbcType="INTEGER"/>
        <result column="label" property="label" jdbcType="VARCHAR"/>
        <result column="deleted" property="deleted" jdbcType="INTEGER"/>
        <result column="creator" property="creator" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="updater" property="updater" jdbcType="VARCHAR"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, member_id, receiver_name, receiver_phone, province, city, district,
        detail_address, is_default, label, deleted, creator, create_time, updater, update_time
    </sql>

    <insert id="insert" parameterType="com.mallease.user.dal.entity.MemberAddress" useGeneratedKeys="true" keyProperty="id">
        insert into user_member_address (member_id, receiver_name, receiver_phone,
                                         province, city, district, detail_address,
                                         is_default, label)
        values (#{memberId,jdbcType=BIGINT}, #{receiverName,jdbcType=VARCHAR}, #{receiverPhone,jdbcType=VARCHAR},
                #{province,jdbcType=VARCHAR}, #{city,jdbcType=VARCHAR}, #{district,jdbcType=VARCHAR},
                #{detailAddress,jdbcType=VARCHAR}, #{isDefault,jdbcType=INTEGER}, #{label,jdbcType=VARCHAR})
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="com.mallease.user.dal.entity.MemberAddress">
        update user_member_address
        <set>
            <if test="receiverName != null">
                receiver_name = #{receiverName,jdbcType=VARCHAR},
            </if>
            <if test="receiverPhone != null">
                receiver_phone = #{receiverPhone,jdbcType=VARCHAR},
            </if>
            <if test="province != null">
                province = #{province,jdbcType=VARCHAR},
            </if>
            <if test="city != null">
                city = #{city,jdbcType=VARCHAR},
            </if>
            <if test="district != null">
                district = #{district,jdbcType=VARCHAR},
            </if>
            <if test="detailAddress != null">
                detail_address = #{detailAddress,jdbcType=VARCHAR},
            </if>
            <if test="isDefault != null">
                is_default = #{isDefault,jdbcType=INTEGER},
            </if>
            <if test="label != null">
                label = #{label,jdbcType=VARCHAR},
            </if>
        </set>
        where id = #{id,jdbcType=BIGINT} and deleted = 0
    </update>

    <update id="logicDeleteById">
        update user_member_address
        set deleted = 1
        where id = #{id,jdbcType=BIGINT}
          and member_id = #{memberId,jdbcType=BIGINT}
          and deleted = 0
    </update>

    <select id="selectByIdAndMemberId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from user_member_address
        where id = #{id,jdbcType=BIGINT}
          and member_id = #{memberId,jdbcType=BIGINT}
          and deleted = 0
    </select>

    <select id="selectByMemberId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from user_member_address
        where member_id = #{memberId,jdbcType=BIGINT}
          and deleted = 0
        order by is_default desc, create_time desc
    </select>

    <select id="selectDefaultByMemberId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from user_member_address
        where member_id = #{memberId,jdbcType=BIGINT}
          and is_default = 1
          and deleted = 0
        limit 1
    </select>

    <select id="countByMemberId" resultType="int">
        select count(*)
        from user_member_address
        where member_id = #{memberId,jdbcType=BIGINT}
          and deleted = 0
    </select>

    <update id="clearDefaultByMemberId">
        update user_member_address
        set is_default = 0
        where member_id = #{memberId,jdbcType=BIGINT}
          and is_default = 1
          and deleted = 0
    </update>
</mapper>
